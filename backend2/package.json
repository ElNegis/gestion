{
  "name": "backend2",
  "version": "1.0.0",
  "description": "Micro-backend simple con Firebase Admin + Express",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --runInBand --coverage",
    "test:ci": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --ci --config=jest.config.cjs"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.0.0",
    "jest-junit": "^13.0.0",
    "supertest": "^6.0.0"
  }
}
